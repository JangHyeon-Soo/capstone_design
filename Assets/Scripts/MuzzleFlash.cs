using UnityEngine;

public class MuzzleFlash : MonoBehaviour
{
    public float muzzleLifeTime;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //Destroy(gameObject ,muzzleLifeTime);
    }
}
